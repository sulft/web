import { CardDirecDirective } from './card-direc.directive';

describe('CardDirecDirective', () => {
  it('should create an instance', () => {
    const directive = new CardDirecDirective();
    expect(directive).toBeTruthy();
  });
});
